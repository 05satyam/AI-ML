# Evals basics

A simple "eval gate" can prevent unreliable outputs:

- **Correctness / sanity**: verify calculator outputs are numeric and within expected bounds.
- **Groundedness**: if the answer uses notes, include citations to the snippet source.
